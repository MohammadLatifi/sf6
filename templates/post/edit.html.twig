{% extends 'admin.base.html.twig' %}

{% block title %}
    Edit Post
{% endblock %}

{% block body %}
    <h1>
        Edit Post
    </h1>
    {{ include('post/_form.html.twig', {'button_label': 'Update'}) }}
    
    <div class='d-flex gap-1 mt-1'>
    <a class='btn btn-primary text-white' href="{{ path('app_post_index') }}">back to list</a>

            {{ include('post/_delete_form.html.twig') }}

        </div>
    </div>
{% endblock %}
